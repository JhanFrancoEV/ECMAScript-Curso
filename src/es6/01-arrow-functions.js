function square(num) {
    return num * num;
}
// Es una funcion Comun


const square = (num) =>{
    return num * num;
}
// Esta es una funcion tipo flecha o arrow function

const square = num => num * num;
// y esta es una funcion tipo flecha o arrow function pero con el return implicito quiere  
// decir que si tienes una sola variable puedes hacerla toda en una sola linea para simplificar
// si hay multiples lineas de codigo dentro de nuestro bloque de codigo es necesario usar los {}
// y return